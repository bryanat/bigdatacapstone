package socketpack

import java.net._
import java.io._

object MainSocketServer {
  def main(args: Array[String]): Unit = {
    println("MainSocketServer is running...")

    var serverSocket = new ServerSocket(6666)
    // this .accept() method is key
    var clientSocketS = serverSocket.accept()
    var outS = new PrintWriter(clientSocketS.getOutputStream(), true)
    var inS = new BufferedReader(new InputStreamReader(clientSocketS.getInputStream()))

    // var server: SocketServerHandler = new SocketServerHandler(6666)

    // var client: SocketClientHandler = new SocketClientHandler("127.0.0.1", 6666);
    // // client.startConnection("127.0.0.1", 6666);
    // var response: String = client.sendMessage("hello server");
    // // assertEquals("hello client", response);

  }
}
